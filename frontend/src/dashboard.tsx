import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "y no gradient" />
      <CardContent> an application that makes gradients but doent sshow em < /CardContent>
      </Card>
    );