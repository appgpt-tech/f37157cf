import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "booklio" />
      <CardContent> A book management system < /CardContent>
      </Card>
    );