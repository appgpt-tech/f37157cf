import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "booksapp" />
      <CardContent> An application to manage books, authors, and genres. < /CardContent>
      </Card>
    );